# Launch Snapshot: 2026-06-17

Generated from the 2026-06-17 UTC metrics run and current GitHub issue state.

## Repository Metrics

```json
{"repo":"morning-verlu/KAI","stars":0,"forks":2,"watchers":0,"socialPreview":true}
```

GitHub traffic:

```json
{"views":{"count":0,"uniques":0},"clones":{"count":2277,"uniques":484},"topReferrers":[],"topPaths":[]}
```

Topics remain aligned with the launch surface:

```text
agent-framework, ai, ai-agent, jvm, kotlin, agent-runtime, ai-infrastructure, kotlin-jvm, multi-agent, workflow-engine, ci-cd, llm, llmops, observability, agent-orchestration, agent-os, ai-agents, evidence-os, llm-observability, kotlin-ai
```

## Open Contributor Surface

- Open PRs: 0.
- #15 is open for an external contributor working on a denied-syscall evidence walkthrough.
- #16 is open for full Docker smoke verification on a normal network.
- #14 is open for a Kotlin API recipe around custom tool capabilities.
- #12 is open because public repository CI still needs a GitHub token with `workflow` scope.

## Auth / CI State

The active GitHub CLI token has:

```text
gist, read:org, repo
```

It does not currently include:

```text
workflow
```

Do not push `.github/workflows/ci.yml` until the maintainer refreshes auth:

```bash
gh auth refresh -h github.com -s workflow
```

The exact enablement, verification, and rollback procedure is documented in
[CI_ENABLE_RUNBOOK.md](../CI_ENABLE_RUNBOOK.md).

## Diagnosis

The bottleneck is still distribution:

- GitHub views are 0.
- Stars are 0.
- X text-first launch did not distribute.
- README, Evidence Viewer, Social Preview, topics, contributor intake, and Docker preflight paths are already prepared.

This means the next useful action is channel change, not more homepage copy.

## Next Operating Actions

1. Publish the Kotlin/JVM community post from [post-now.md](../launch-posts/post-now.md).
2. After posting, record the URL and run `./scripts/launch-metrics.sh`.
3. If GitHub views stay at 0 after that post, move to Show HN or another higher-surface community instead of editing README again.
4. Watch #15 for a contributor PR or follow-up question.
5. Ask the maintainer to refresh `workflow` scope so #12 can become real public CI.
6. Ask a contributor or external machine with normal Docker Hub speed to run `./scripts/docker-smoke.sh` for #16.

Channel priority and source notes are tracked in [community-targets.md](../launch-posts/community-targets.md).

## Current Best Link

For cold audiences:

```text
https://morning-verlu.github.io/KAI/evidence-viewer.html
```

For Kotlin/JVM communities, use the tracked post-now link:

```text
https://morning-verlu.github.io/KAI/evidence-viewer.html?utm_source=kotlin_community&utm_medium=community&utm_campaign=post_now
```
